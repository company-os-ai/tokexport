# --- Public (visible in browser; set via Vercel env) ---
NEXT_PUBLIC_PRODUCT_NAME="My Launch"
NEXT_PUBLIC_PRODUCT_TAGLINE="A one-shot product."
NEXT_PUBLIC_INPUT_LABEL="Your input"
NEXT_PUBLIC_INPUT_PLACEHOLDER="example"
NEXT_PUBLIC_BUY_BUTTON_TEXT="Buy for $1"
NEXT_PUBLIC_STRIPE_PAYMENT_LINK="https://buy.stripe.com/xxxxxxxxxxxx"

# --- Stripe (server) ---
# Used to verify the checkout session was paid before serving the download.
STRIPE_API_KEY="sk_live_..."

# --- Per-product fulfillment ---
# Add product-specific env vars here (API keys, URLs, etc.) and consume them
# in lib/fulfillment.ts.
